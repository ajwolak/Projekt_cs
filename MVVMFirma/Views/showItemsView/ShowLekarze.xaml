<local:AllShowViewBase x:Class="MVVMFirma.Views.showItemsView.ShowLekarze"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views.showItemsView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid Background="White" AutoGenerateColumns="False"  ItemsSource="{Binding List}">
            <DataGrid.Columns >
                <DataGridTextColumn Header="Id lekarza" Binding="{Binding lekarzId}"/>
                <DataGridTextColumn Header="Imię lekarza" Binding="{Binding lekarzImie}"/>
                <DataGridTextColumn Header="Nazwisko Lekarza" Binding="{Binding lekarzNazwisko}"/>
                <DataGridTextColumn Header="Nazwa specjalizacji" Binding="{Binding nazwaSpecjalizacji}"/>
                <DataGridTextColumn Header="Telefon" Binding="{Binding telefon}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding email}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllShowViewBase>
